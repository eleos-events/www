# Claude Code Project Guidelines

## Screenshots

When taking screenshots (via Playwright or any other tool), always save them to `.claude/screenshots/` using a descriptive filename. Do not save screenshots to the project root or other directories.

Example: `.claude/screenshots/mobile-menu-open.png`
